# Oracle Base Database on OCI

## Connect 
Use the public IP address to connect to the system from outside the cloud (with no VPN). You can find the IP addresses in the Console as follows:
1. On the DB System Details page, under **Resources**, click **Nodes**.
2. The Public IP address and Private IP address & DNS name are displayed in the table columns.
